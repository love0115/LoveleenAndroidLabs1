<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LoveleenAndroidLabs" parent="Theme.Material3.DayNight">

        <!-- Primary  brand color, -->
        <item name="colorPrimary">@color/my_primary</item>

        <!-- Secondary brand color, -->
         <item name="colorSecondary">@color/my_secondary</item>

        <!-- text  brand color, -->
        <item name="android:textColor">@color/my_text</item>

        <!-- Statusbar color,-->
        <item name="android:statusBarColor">@color/my_statusbar</item>


    </style>

    <style name="Theme.LoveleenAndroidLabs" parent="Base.Theme.LoveleenAndroidLabs" />
</resources>